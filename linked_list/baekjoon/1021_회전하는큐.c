#include <stdio.h>
#include "malloc.h"
int n, m;
int check_list[60];

typedef struct node {
	int data;
	int number;
	struct node *next;
}node;
node *head;
node *tail;

void insert_node() {

}
int main() {
	freopen("1021_input.txt", "r", stdin);
	int i;
	scanf("%d %d", &n, &m);
	for (i = 0; i< m)
	return 0;
}
